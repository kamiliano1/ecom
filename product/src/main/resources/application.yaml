spring:
  application:
    name: product-service
  datasource:
    url: jdbc:h2:mem:test
server:
  port: 8081
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
