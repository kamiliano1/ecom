spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:test
server:
  port: 8083
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
