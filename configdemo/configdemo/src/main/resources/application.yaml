spring:
  application:
    name: configdemo
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888

localfile:
  id: 122
  version: 2.2.2
  name: "Default-Build"
  type: "Default Build"
  placeholder: ${INTELLIID}

management:
  endpoints:
    web:
      exposure:
        include: refresh